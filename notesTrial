class NotesApplication {
	constructor (author) {
		this.author = author;
		this.notes = [];
	}
}
create(note_content)
this.notes[note_content].push(notes)

listNotes(note_id, [NOTE_CONTENT], [author])

get(note_id)
return 

search(search_text) 

delete(note_id); 

edit(note_id, new_content)